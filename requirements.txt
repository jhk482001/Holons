flask==3.0.3
flask-cors==4.0.1
boto3==1.34.144
mysql-connector-python==9.0.0
python-dotenv==1.0.1
werkzeug==3.0.3

# v2 (Postgres migration) — not yet used by running code
psycopg[binary,pool]==3.2.1
pgvector==0.3.0

# Used by backend.services.asset_crypto (Fernet) to encrypt
# MCP / RAG connection credentials at rest.
cryptography>=41,<46

# Cron expression parsing for the scheduler. Without this,
# backend.services.scheduler falls back to a 1-hour interval for any
# cron-trigger schedule — so interval triggers keep working but cron
# ones silently misfire.
croniter>=2,<7
